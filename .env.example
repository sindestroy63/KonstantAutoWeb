# Server-only endpoint that accepts POST { "text": "..." }.
LEADS_PROXY_URL=https://proxy.example.com/api/site-lead

# Shared Bearer token for the leads proxy. Never prefix this variable with NEXT_PUBLIC_.
LEADS_PROXY_SECRET=replace-with-a-long-random-secret
